## [v0.1.8] - 2026-08-22

### Features
- **Web 表情包管理面板**：新增「表情包管理」页签，把原本只能由大模型自动收藏的表情包库完整开放出来管理。
  - 画廊浏览（缩略图懒加载，悬停换原图以查看 GIF 动画）、按归属机器人 / 分类 / 标签 / 关键词筛选、点击徽章即筛。
  - 编辑名称、分类、标签、描述——这三项是大模型挑选表情包的唯一依据，改完即时生效无需重载。
  - 手动上传（点击或拖拽、支持批量），`sticker_id` 沿用与自动收藏同源的 xxh3 图片哈希，同图天然去重不会重复入库。
  - 可选 AI 自动分析（复用现有视觉模型链路填写元信息，默认关闭以免意外消耗 Token）；亦可对已有表情包重新分析。
  - 机器人归属增删，可把一个机器人收藏的表情包共享给另一个。
  - 批量改分类 / 追加或移除标签 / 绑定解绑机器人 / 删除；分类与标签支持重命名及同名合并。
- **表情包删除改为彻底清理**：原先 `delete_sticker` 只删 `stickers` 一行，会在 `stickers_bot` 里残留失效 ID、在磁盘上残留孤儿图片。现在同时清理数据库记录、所有机器人的归属引用、本地图片与缩略图。

### Bug Fixes
- **修复编辑表情包元数据会清空图片文件名的隐患**：`insert_sticker` 是全字段 upsert 且 `filename` 默认空串，用它改元信息会导致 `get_sticker_path()` 返回 None、机器人再也发不出该表情包。新增只更新元数据的 `update_sticker` 供 Web 端使用。
- **修复内存缓存与数据库不一致**：`EmojiManager.stickers` / `bot_stickers` 是长驻缓存，提示词注入与发图都直接读它。所有 Web 写操作现在都会同步缓存（批量操作则统一失效重载），避免改完之后机器人仍按旧数据发图。

## [v0.1.7] - 2026-08-21

### Improvements & Bug Fixes
- **视频本地路径探测误报修复**：优化 `video_utils` 本地视频解析判断，只有符合本地文件路径/`file://` 特征的候选者才进行安全沙箱探测，避免 OneBot 纯文件名触发非安全目录拦截告警。
- **单条消息多图与群聊突发图潮限流优化**：
  - 单条消息最多自动转述第 1 张图片，第 2 张及后续图片自动转为延迟转述（`defer_caption`），瞬间完成占位，彻底解决多图发送时的长响应延迟；
  - 增加会话级分钟滑动窗口频控（每分钟最多 8 张），自动熔断群内刷图或密集斗图造成的 LLM 队列积压；
  - 被延迟转述的图片依然支持 Bot 在对话中通过 `inspect_media` 工具按需查阅。

## [v0.1.6] - 2026-08-21

### Security & Improvements
- **媒体本地文件读取沙箱隔离**：限制 `file://` 及本地媒体读取仅允许访问系统临时目录（`temp`）、插件缓存（`media_cache`）与 AstrBot 临时目录（`data/temp`），防范越权读取敏感文件。
- **外部网络媒体下载请求头优化**：为媒体下载补充标准浏览器 `User-Agent` 与自源动态 `Referer`，解决部分第三方网站（如 Wiki、Pixiv、B站等）防盗链触发 403 下载失败的问题。
- **数据库表名安全校验**：在 `/删除数据表` 命令中增加表名合法性校验，避免非预期 SQL 语法错误与注入风险（虽然不太可能，该指令权限为管理员）。

## [v0.1.5] - 2026-08-21

### Bug Fixes & Stability
- **CPU AVX2 指令集前置检测**：在插件启动阶段自动检测宿主 CPU 是否支持 AVX2 指令集；在不支持的老旧 CPU（如 AMD Piledriver / Intel Sandy Bridge）上主动抛出异常终止插件加载，彻底避免加载底层 LanceDB C 扩展时触发非法指令（SIGILL 132）导致 AstrBot 闪退和 Docker 无限重启循环。

## [v0.1.4] - 2026-08-20

### Features & Improvements
- **`<image>` 标签解析支持**：支持 LLM 输出 `<image url="..."/>` XML 标签发送网络图片。

## [v0.1.3] - 2026-08-19

### Bug Fixes
- 修复空消息判定中的 NameError 并清理工具递归实参

## [v0.1.2] - 2026-08-19

### 性能优化与提示词重构

- **Prompt Caching 架构重构**
  - 重构小模型决策与正式回复的用户提示词结构，严格遵循 **Static-to-Dynamic（从静态到动态）** 渐进式排版；
  - 将群数据、群画像及短期任务看板等稳固基石置顶，将高频变动的时间戳与状态置底，最大化保护前缀缓存链路，使大模型（DeepSeek、Claude、OpenAI 等）提升上下文缓存命中率。
- **小模型决策轻量化与防盲盒优化**
  - 小模型决策上下文消息条数独立固定为 12 条，大幅降低 Token 消耗并加速响应；
  - 决策提示词支持全量内联本地缓存的媒体转述，解决表情包与图文消息的误判盲盒问题。

## [v0.1.0] - 2026-08-18 (Preview)

**Giftia 首个公测预发布版本** 一款专注于人设陪伴、多轮记忆与拟人社交的 AstrBot 插件。

### 核心特性

- **拟人人格与智能决策**
  - 基于轻量小模型实现智能对话审查与主动接话决策，支持灵活的触发概率、白名单及关键词过滤规则。
  - 支持多 Bot 独立管理，分别配置专属人格、模型优先级及 TTS 语音。
- **记忆沉淀与关系画像系统**
  - **多轮记忆与 RAG 检索**：结合向量嵌入（Embedding）与重排（Rerank）模型，实现精准的长期记忆关联。
  - **动态画像与好感度**：支持被动总结聊天记忆，自动更新用户画像与好感度演化。
- **媒体缓存与低 Token 多模态转述**
  - 持久化媒体缓存与 16 位 Hash 索引，解耦大模型与原始媒体传递。
  - 支持图片、语音及视频的智能压制与多模态转述，提供 `inspect_media` 按需查阅，降低上下文 Token 消耗。
- **拟人社交动作与群通知机制**
  - 丰富互动动作支持：戳一戳、名片点赞、表情包发送、合并转发等。
  - 群通知结构化解析入库（进群/退群/管理员变动等），支持群禁言捕获、全链路静默与据点哭诉告状机制。
- **可视化 Web 仪表盘**
  - 提供直观的 Web 管理控制台，轻松管理机器人、会话记忆、用户画像与媒体缓存。