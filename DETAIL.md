# Giftia 详细使用文档

本文件包含 Giftia 插件的完整命令列表、核心配置项详解、提示词示例说明以及故障排查指南。

---

## 常用命令列表

以下命令通常需要**管理员权限**（除部分群组/非敏感查询外）：

| 命令 | 参数 | 描述 | 示例 |
| :--- | :--- | :--- | :--- |
| `/工具列表` | `[页码]` | 列出当前 Bot 已加载的 LLM 工具（函数调用列表） | `/工具列表 1` |
| `/工具解析` | `<工具名>` | 将特定工具转换为大模型可用的 XML 格式 | `/工具解析 search_chat_history` |
| `/读取记忆` | `<Bot名> <群/用户ID> <查询词>` | 根据 RAG 检索长期向量记忆 | `/读取记忆 流萤 1234567 暑假约定` |
| `/读取近期记忆` | `<Bot名> <群/用户ID> [数量]` | 查看该会话下的最新几条短期聊天总结 | `/读取近期记忆 流萤 1234567` |
| `/强制总结` | 无 | 手动强制触发对当前会话未总结消息的 passive 总结 | `/强制总结` |
| `/删除消息` | 无 | 引用某条消息发送此命令，将从本地缓存中彻底删除 | 引用消息后发送：`/删除消息` |
| `/删除记忆` | `<记忆UUID>` | 删除一条特定的长期记忆 | `/删除记忆 xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx` |
| `/清空记忆` | `<Bot名> <群/用户ID>` | 彻底清空某会话下该 Bot 的全部记忆 | `/清空记忆 流萤 1234567` |
| `/加满能量` | `<Bot名>` | 手动加满该 Bot 在当前会话的能量值（对应能量衰减限制） | `/加满能量 流萤` |
| `/清空媒体缓存` | 无 | 清空转述缓存数据库及对应的多媒体文件 | `/清空媒体缓存` |
| `/定时任务列表`| `[页码]` | 列出所有由大模型通过工具创建的定时提醒任务 | `/定时任务列表 1` |
| `/获取定时任务`| `<前缀>` | 按 Bot 或群 ID 前缀过滤定时任务 | `/获取定时任务 流萤:1234` |
| `/删除定时任务`| `<任务ID>` | 取消并删除对应的定时提醒任务 | `/删除定时任务 job_xxxxx` |
| `/读取媒体转述`| 无 | 引用图片/语音或当前消息中携带媒体时，查看对其识别转述的内容 | `/读取媒体转述`（别名 `/媒体转述`）|
| `/设置常驻状态` | `<状态名> [状态值]` | 设置或删除当前会话Bot的常驻状态（仅管理员） | `/设置常驻状态 服装 女仆装` 或 `/设置常驻状态 服装`（删除） |
| `/查看状态` | 无 | 查看当前会话Bot的临时状态与常驻状态（所有人可用，别名 `/状态`） | `/查看状态` 或 `/状态` |
| `/静默` | 无 | 将当前会话Bot的状态设置为不活跃并重置接话窗口（仅管理员，别名 `/休眠`、`/闭嘴`） | `/静默` 或 `/休眠` |

---

## 核心配置详解

你可以在后台插件配置（或 `_conf_schema.json` 自动生成的配置界面）中修改以下自定义参数。

### 1. 机器人模板列表 (`bot_template`)
可以为同一个 AstrBot 实例添加多个**机器人设定**。每个机器人的记忆和画像是物理隔离的。
- **启用机器人 (`enabled`)**: 是否启用该机器人实例。
- **机器人名称 (`name`)**: 机器人名称，如 `流萤`。名称必须唯一，用于索引机器人配置（支持中文）。
- **机器人昵称 (`nickname`)**: 机器人昵称，用于聊天历史和消息记录中的识别。
- **适配器ID列表 (`adapter_ids`)**: 绑定的平台/账号 ID（通过 `/sid` 查看），支持多平台共用同一记忆。

### 2. 小模型接话决策 (`decision_conf`)
- **启用小模型判断功能 (`enabled`)**: 推荐开启。开启后会先经过高性价比的小模型对上下文进行是否回复的决策。
- **小模型提供商 (`provider_ids`)**: 建议选择低延迟、低单价的大模型（支持配置多个以自动降级 fallback）。
- **接话分析窗口消息数 (`reply_active_window`)**: 机器人发言后，后续多少条群消息会触发分析。设置为 `0` 表示只在被 `@` 时才回复。
- **主动接话触发概率 (`proactive_probability`)**: 当分析计数为 0 且没有被 `@` 时，群友发来的每条消息按设定概率（0-100%）随机触发接话决策。
- **启用关键词监听模式 (`keyword_trigger_enabled`)**: 开启后，当群聊消息中提到设置的兴趣关键词时触发接话决策。
- **兴趣关键词列表 (`keyword_rules`)**: 支持直接输入关键词，或以“关键词:概率”格式（例如 `原神:50`）指定独立触发概率。
- **小模型判断系统提示词 (`decision_prompt`)**: 控制小模型判断逻辑的系统提示词（模版见 [流萤-小模型判断.md](prompt_example/character/流萤-小模型判断.md)）。

### 3. 大模型回复配置 (`llm_reply_conf`)
- **启用大模型回复功能 (`enabled`)**: 是否启用大模型回复。
- **大模型回复提供商 (`provider_ids`)**: 你的核心回复大模型，支持配置多个以自动降级或随机轮询。
- **多提供商选择模式 (`provider_selection_mode`)**: 支持 `fallback`（按顺序降级尝试）或 `random`（随机打乱顺序使用）。
- **关联 AstrBot 人格 (`persona_id`)**: 直接绑定 AstrBot 控制台的人格配置，回复时自动抽取该人格的 System Prompt 以及工具能力限制 (`tools`)。

### 4. 媒体转述配置 (`caption_config`)
- **启用图片转述 (`image_caption_enabled`)** / **启用音频转述 (`audio_caption_enabled`)**: 是否开启对多媒体消息的自动多模态识别与文字翻译。
- **图片/音频转述提供商 (`image_caption_provider_ids` / `audio_caption_provider_ids`)**: 用于执行多模态转述的大模型通道。
- **非活跃时媒体不自动转述 (`defer_caption_enabled`)**: 开启后，在非活跃群聊中收到的图片/语音消息不立刻调用大模型转述，仅下载缓存并保留占位符 ID，Bot 在需要时可通过 `inspect_media` 工具主动查看，彻底消除唤醒时的多模态延迟并大幅节省 Token。

### 5. 记忆检索配置 (`memory_config`)
- **启用嵌入模型 (`embedding_conf.enabled`)**: 是否启用本地或外部 Embedding 提供商，开启后才能使用长期记忆检索功能（注：底层向量库 LanceDB 在 x86 架构下需要 CPU 支持 AVX2 指令集；若不支持，插件在启动时会检测并抛出异常中止加载以保护 AstrBot 本体稳定运行；若确信误报可配置 `ignore_avx2_check` 强制跳过）。
- **启用重排模型 (`rerank_conf.enabled`)**: 是否启用 Rerank 提供商对检索到的向量记忆进行重排，以获取更精确的参考。
- **启用被动状态维护 (`passive_memory_enabled`)**: 开启后台闲聊自动总结归纳。机器人会在后台对聊天片段进行总结，动态更新用户画像、群画像、好感度与关系头衔。
- **被动总结模型提供商 (`passive_memory_provider_ids`)**: 指定用于后台总结与画像提取的大模型通道。
- **被动总结静默触发阈值 (`passive_memory_silence_threshold`)**: 机器人发言后，群聊沉寂静默多少条消息后触发后台被动总结。
- **被动总结溢出触发阈值 (`passive_memory_overflow_threshold`)**: 机器人活跃时，未处理缓存消息累计达到多少条后触发后台总结。

### 6. 消息历史与屏蔽配置 (`msg_history`)
- **消息历史数量 (`msg_number`)**: 会话上下文中维持的单会话最大历史消息条数。
- **指令消息入库屏蔽 (`block_command_messages`)**: 默认关闭。开启后，用户发送的 AstrBot 指令以及 Bot 对指令的回复均不会存入聊天记录数据库，保持聊天记忆干净整洁。无论此开关是否开启，插件内部的 `/删除消息` 指令及其响应均强制屏蔽入库。

---

## 提示词示例文件说明

插件附带的 `prompt_example` 目录提供了丰富的人设与判断 Prompts 供参考：

### 核心配置提示词（主要 Bot 人设与决策参考，建议复制并填入插件配置中）
- [天童爱丽丝-大模型回复.md](prompt_example/character/天童爱丽丝-大模型回复.md) / [流萤-大模型回复.md](prompt_example/character/流萤-大模型回复.md)：大模型生成回复所需的人设提示词参考。
- [天童爱丽丝-小模型判断.md](prompt_example/character/天童爱丽丝-小模型判断.md) / [流萤-小模型判断.md](prompt_example/character/流萤-小模型判断.md)：控制小模型回复决策与何时触发 RAG 记忆检索的逻辑判断提示词参考。

### 内置硬编码提示词（仅供查看）
以下提示词已硬编码嵌入本插件底层，**无需且不支持手动配置**，对应的示例文件仅供开发者与用户查看和了解其内部工作原理：
- [用户画像维护.md](prompt_example/memory/用户画像维护.md) & [关系画像维护.md](prompt_example/memory/关系画像维护.md) (画像被动记忆归纳)
- [图片转述.md](prompt_example/quote/图片转述.md) & [音频转述.md](prompt_example/quote/音频转述.md) (多模态转述规则)
- [记忆总结.md](prompt_example/memory/记忆总结.md) (短期归纳总结)
- [表情包分析.md](prompt_example/quote/表情包分析.md)

---

## Web 管理面板

在 AstrBot 插件页面打开插件 WebUI 即可进入，包含以下页签：决策审计、长期记忆、会话状态、媒体转述、**表情包管理**、合并转发、画像管理、Token统计、机器人管理。

### 表情包管理

表情包原本只能由大模型自己收藏（它看到有意思的图片时输出 `<add_sticker>`，再由视觉模型判断是否值得收录并自动生成名称/分类/标签）。这个页签把整个表情包库摊开来给你直接管理。

**为什么元信息很重要**：机器人发表情包时，提示词里注入的是 `[名称](sticker_id) - 分类: X, 标签: Y`——名称、分类和标签是它挑图的唯一依据。名字起歪了它就会一直发错场合，在这里改准即可，改完立刻生效，无需重载插件。

| 功能 | 说明 |
| :--- | :--- |
| 画廊浏览 | 缩略图网格展示，鼠标悬停约 0.5 秒后自动换成原图（GIF 可看动画） |
| 多维筛选 | 按归属机器人 / 分类 / 标签 / 关键词（名称、描述、ID）筛选；点卡片上的分类徽章或标签也能直接筛 |
| 编辑 | 修改名称、分类、标签、描述，以及归属哪些机器人 |
| 手动上传 | 支持点击选择或拖拽，可一次多张。支持 PNG / JPEG / GIF / WEBP / BMP，单张上限 10MB，单次上限 50 张 |
| AI 自动分析 | 上传时可勾选，或在编辑弹窗点「重新 AI 分析」，复用与自动收藏相同的视觉模型来填写元信息。**每张图都会消耗 Token**，默认关闭 |
| 归属管理 | 表情包按机器人隔离（`stickers_bot` 表）。可把 A 机器人收藏的表情包共享给 B，或取消某个机器人的归属 |
| 批量操作 | 勾选多张后批量改分类 / 追加标签 / 移除标签 / 绑定或解绑机器人 / 删除 |
| 分类标签管理 | 查看各分类与标签的表情包数量，支持重命名；重命名为已存在的名字即为**合并** |
| 以 GIF 格式发送 | 页签顶部的开关，**每个机器人独立**。开启后该机器人发表情包前统一转成 GIF，切换即时生效无需重载插件。详见下方说明 |

**关于「以 GIF 格式发送」**：

这个开关主要是给**官方 QQ** 用的。官方 QQ 不支持 OneBot 的小图表情包外显（`subType=1`），表情包会被当成普通大图发出，在会话窗口里占掉大量空间；转成 GIF 后客户端便按表情包渲染，观感与占位都接近真表情包。OneBot 平台本就支持小图，开启后只是观感更贴近真表情包。面板会给绑定了官方 QQ 适配器的机器人标注「建议开启」。

- **代价**：转换结果缓存在 `stickers/gif_cache/`，同一张表情包只在首次发送时消耗一次 CPU，之后直接复用；磁盘上限就是表情包库的大小。删除表情包时该缓存会一并清理。
- **自动跳过与回退**：源文件本身是 GIF 的不重编码（避免量化与砍帧损失）；转换失败或产物超过 10MB 时自动回退按原文件发送，不会中断回复。动图最多保留 30 帧。
- **只作用于表情包**：其他插件产出的绘图/生图大图、`<image url=...>` 网络图都不受影响。入库的聊天记录仍记原 `sticker_id`，不受 GIF 转换影响。
- **已知限制**：显示「图片文件缺失」、发送时回退到媒体缓存远程 URL 的表情包无法转 GIF（不在发送热路径上额外做一次网络下载）。

**几点注意**：

- **上传去重**：`sticker_id` 由图片内容的 xxh3 哈希算出，与自动收藏链路同源。上传一张已存在的图会提示「已存在」而不会重复入库或覆盖原有元信息；若同时勾了归属机器人，则只补归属。
- **未归属的表情包不会被发送**。卡片上显示 `🤖 未归属` 的表情包虽然在库里，但没有任何机器人会用它，需要在编辑弹窗或批量操作里绑定机器人。
- **删除是彻底的**：会同时清理数据库记录、所有机器人的归属引用、本地图片文件、缩略图与 GIF 缓存，不可恢复。
- **图片缺失**：卡片显示「图片文件缺失」表示数据库有记录但本地文件已丢失（通常是历史上下载失败）。这类表情包发送时会尝试回退到媒体缓存里的原始 URL。
- **未配置视觉模型时**，AI 分析相关按钮会置灰，其余功能不受影响。对应配置项为 `caption_config` 下的 `image_caption_provider_ids`。

---

## 故障排查与说明

- **数据库锁定/报错**：本插件底层使用 `LanceDB` 存储向量记忆与 SQLite。请确保插件数据目录下文件可读写。若数据库出现异常损坏，可尝试通过命令删除特定的表或手动清空缓存。
- **接话过于频繁**：请调低小模型的 `主动接话触发概率`，调小 `接话分析窗口消息数`，并合理设置 `并发配置` 下的 `群组节流时间` 和 `用户防抖`。
- **机器人发的表情包总是对不上场合**：到 Web 面板「表情包管理」页签检查该表情包的名称、分类与标签是否准确——这三项是大模型挑图的唯一依据。改完即时生效。
